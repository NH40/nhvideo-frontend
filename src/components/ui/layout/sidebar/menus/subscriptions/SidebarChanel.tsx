import type { FC } from 'react'

export const SidebarChanel: FC = () => {
  return (
    <div>
      <ul>
        {/* {menu.map(item => (
          <ChanelItem
            item={item}
            key={item.label}
          />
        ))} */}
      </ul>
    </div>
  )
}
